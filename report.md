# SEEM3650 Practical Exam Report
**Student ID:** 1155170951

## Step 2: Shakespeare Samples
(Paste your 5 lines here)
Nurse:
My fairest with it our mine honour.

LEONTES:
What shall she seem to him the feelir of your honour?

## Step 3: Model Architecture
* **Lowest Validation Loss:** 1.5273
* **Best Settings:** 7 layers, 4 heads

## Step 4: Code Generation
* **Total Tokens:** 127917
...

   u handle = RemovableHandle(_global_buffer_registration_hooks[handle.id] = hook
   return handle

def registration_hook(
     hook: Callable[["Module", _grad_t], _grad_t | None],
) -> RemovableHandle:
     r"""Register a backward hook common to all the modules.

    .. warning ::

     This addds global state_dict pre-hook to be calleds and
       registered with_module buffer to :meth:`torch.nn.Module.register_full_backward_hook`.

     Returns:
            a handle the are commmon to rke th
---------------

                self._parameters[name] = param

        param if params is None:
            param is None:
                param.grad = param.requires_grad.detach(
                               param.grad.data = target.push_use_set_data(param)
                 self._parameters[name] = name
                    if not isinstance(result, torch.nn.param):
                              else:
                                        else:
                                                              

